{
  "name": "olesia-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Olesia Bitcoin wallet — iOS (Capacitor wrapper that bundles the web wallet locally)",
  "scripts": {
    "prepare-www": "node prepare.mjs",
    "sync": "node prepare.mjs && npx cap sync ios",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
